import CreateGame from "@/app/[gameId]/CreateGame";

export default function Home() {
    return (
        <main className="animate-fade-in w-full max-w-md mx-auto">
            <CreateGame />
        </main>
    );
}
